{
  "fixtures": {
    "request_emoji_body.json": "worst-case graphemes on the wire: ZWJ family, flag, skin tone, combining accent",
    "request_full.json": "every optional field populated, including a full outbox and kipu",
    "request_heartbeat.json": "the normal empty sync: cursor and version only (server §4.2)",
    "request_window_resync.json": "empty cursor means window resync; the device never parses a cursor (server §4.4)",
    "response_all_ack_statuses.json": "every terminal ack the device must handle, including forward-compat unknown (D-5)",
    "response_ayllu_and_config.json": "contact block with a tombstone and c_sys, plus pushed config (server §7.2, A.15)",
    "response_letters.json": "inbound letters with each flag combination the device renders differently",
    "response_more_true.json": "the drain loop: device syncs again immediately, capped at 10 rounds (server §4.6)"
  },
  "note": "Generated by tools/wirefixtures from internal/protocol. Do not hand-edit. Regenerate on any wire change; the firmware's C++ mirror is asserted against these."
}
