/wasi
/dist/
*.test
.env
/data/
/backups/
__pycache__/
*.pyc
chaskisim-state.json

# Firmware build trees
/build/
firmware/chaski/build/
firmware/chaski/build-*/
firmware/chaski/sdkconfig
firmware/chaski/sdkconfig.old
firmware/chaski/managed_components/
# dependencies.lock is deliberately NOT ignored: it is the only exact pin on the
# vendor components, since the manifest carries ranges. See "Pinned versions" in
# firmware/chaski/README.md.

# Editor swap files
*.swp
*.swo
