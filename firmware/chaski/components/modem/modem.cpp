// Implementation of components/modem — the ONLY caller of walter-modem.
// Target-only: excluded from the host build, since it wraps vendor hardware
// code. Wave 4 (CM3) fills it in.
#include "chaski/modem.h"
